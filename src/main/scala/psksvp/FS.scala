package psksvp

object FS
{
  /*
  class Path(components:Seq[String])
  {
    def apply(i:Int):String =
    {
      assert(i >= 0 && i < components.length, s"Path.apply($i), index $i of of bound")
      components(i)
    }

    override def toString: String =
    {

    }
  }

  object Path
  {
    def join(component:String, components:String*):Path = new Path(component :+ components.flatten)
  }*/
}
